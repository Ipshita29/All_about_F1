# Optional — base URL of the backend API. Without this, the app talks to
# http://localhost:3000 (the server's local dev default). Set this when
# deploying the frontend separately from the backend (e.g. Vercel), pointed
# at your deployed backend's URL (no trailing slash).
VITE_API_URL=
