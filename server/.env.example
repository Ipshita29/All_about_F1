PORT=3000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret
NEWS_API_KEY=your_newsapi_key

# No credentials needed for live race data — /api/live/race uses F1's public
# live timing feed (see "Live Data Architecture" in the root README) and
# falls back to Jolpica schedule data whenever no session is live.
